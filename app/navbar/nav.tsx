import NavMenu from "./navMenu";

const Nav = () => {
  return (
    <nav className="flex items-center justify-center w-full h-[80px] bg-gradient-to-t from-banner-start to-banner-end">
      <NavMenu />
    </nav>
  );
};

export default Nav;
